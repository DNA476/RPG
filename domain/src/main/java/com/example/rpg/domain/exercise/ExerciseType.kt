package com.example.rpg.domain.exercise

/**
 * Exercise identity used to map movement repetitions to attacks and progression rewards.
 */
enum class ExerciseType {
    SQUAT,
    PUSH_UP,
    PULL_UP,
    CRUNCH,
    LUNGE,
    JUMPING_JACK,
    PLANK,
}
